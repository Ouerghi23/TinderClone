import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourcompany.tinderclone',
  appName: 'TinderClone',
  webDir: 'dist'
};

export default config;
